# Core parsing/storage/alerting -- unchanged from the original logtool/ implementation
pydantic>=2.5.0
python-dateutil>=2.8.2
sqlalchemy>=2.0.0
pyyaml>=6.0
requests>=2.31.0

# Auth
bcrypt>=4.1.0

# API layer
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9   # required by FastAPI for file upload form parsing

# Remote machine log collection (SSH/SFTP pull)
paramiko>=3.4.0
cryptography>=42.0.0
apscheduler>=3.10.0

# Testing
pytest>=8.0.0
pytest-mock>=3.12.0
httpx>=0.27.0               # required by fastapi.testclient.TestClient
httpx2>=2.0.0                # newer Starlette/FastAPI versions require this too (separate package, not a typo)
